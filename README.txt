A constraint solver for whose primitive constraints assert membership
in a binary relation:

 x R y

where 'R' is a binary relation on a domain D, and 'x' and 'y' are
either variables or values from D.
